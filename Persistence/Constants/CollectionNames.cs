namespace Persistence.Constants;

internal static class CollectionNames
{
    internal const string CourseMaterials = nameof(CourseMaterials);
    internal const string SubmittedAssignments = nameof(SubmittedAssignments);
    internal const string Grades = nameof(Grades);
}
